[project]
name = "packet-sniffer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.1"
dependencies = [
    "pyshark>=0.6",
]

[dependency-groups]
dev = [
    "ruff>=0.13.0",
]
